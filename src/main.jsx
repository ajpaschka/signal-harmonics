import React from "react";
import ReactDOM from "react-dom/client";
import HarmonicLatch from "./harmonic-latch.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <HarmonicLatch />
  </React.StrictMode>
);
